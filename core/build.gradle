apply plugin: "java"

sourceCompatibility = 1.11
[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

sourceSets 
{
    main 
    {
        java 
        {
            srcDir 'src'  
        }
    }
}
eclipse.project {
    name = appName + "-core"
}

